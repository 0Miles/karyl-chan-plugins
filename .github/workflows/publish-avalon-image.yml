name: Publish avalon image

on:
  push:
    branches: [main]
    tags:
      - 'avalon-v*'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: ${{ github.repository_owner }}

jobs:
  publish:
    name: Push avalon image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute image tags
        id: tags
        run: |
          OWNER_LC=$(echo "${{ env.IMAGE_OWNER }}" | tr '[:upper:]' '[:lower:]')
          IMG="${{ env.REGISTRY }}/${OWNER_LC}/karyl-plugin-avalon"
          REF="${GITHUB_REF}"
          if [[ "$REF" == refs/tags/avalon-v* ]]; then
            # tag form: avalon-v<semver>  → :<semver> + :latest
            VERSION="${REF#refs/tags/avalon-v}"
            echo "tags=$IMG:$VERSION,$IMG:latest" >> "$GITHUB_OUTPUT"
          else
            # push to main / manual dispatch → :edge AND :latest
            # (:latest mirrors the bot's docker-publish.yml convention so a
            #  watchtower-pinned prod deployment auto-updates from main).
            echo "tags=$IMG:edge,$IMG:latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.avalon
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            karyl.plugin.key=avalon
          cache-from: type=gha,scope=plugin-avalon
          cache-to: type=gha,mode=max,scope=plugin-avalon
