/** Plugin key (= manifest plugin.id). Single source of truth — also the
 *  pluginKey half of `plugin:<key>:webui.access` and of the
 *  `kc:<key>:<action>` button custom ids. */
export const PLUGIN_KEY = "karyl-radio";
